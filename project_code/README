1. host_tolerance.py : This is the python code file which will be run in host machine. It will run as a service. 
			"python host_tolerance.py &"
			
			
2. controller_tolerance.py : This is the python code file which will be run in controller machine. It will run as a service. 
				For more information refer thesis report 
			
3. load_balancer.py : This is load balancing module run in controller machine.
			for more information refer thesis report		

4. server_tolerance.py : This module runs on controller machine by task scheduler.
